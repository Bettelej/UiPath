<Activity mc:Ignorable="sap sap2010" x:Class="Export_Queues" VisualBasic.Settings="{x:Null}" sap2010:WorkflowViewState.IdRef="ActivityBuilder_1"
  xmlns="http://schemas.microsoft.com/netfx/2009/xaml/activities"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:sap="http://schemas.microsoft.com/netfx/2009/xaml/activities/presentation"
  xmlns:sap2010="http://schemas.microsoft.com/netfx/2010/xaml/activities/presentation"
  xmlns:scg="clr-namespace:System.Collections.Generic;assembly=System.Private.CoreLib"
  xmlns:sco="clr-namespace:System.Collections.ObjectModel;assembly=System.Private.CoreLib"
  xmlns:ui="http://schemas.uipath.com/workflow/activities"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <TextExpression.NamespacesForImplementation>
    <sco:Collection x:TypeArguments="x:String">
      <x:String>System</x:String>
      <x:String>System.IO</x:String>
      <x:String>System.Collections.Generic</x:String>
      <x:String>Newtonsoft.Json</x:String>
      <x:String>Newtonsoft.Json.Linq</x:String>
      <x:String>UiPath.Core</x:String>
    </sco:Collection>
  </TextExpression.NamespacesForImplementation>
  <TextExpression.ReferencesForImplementation>
    <sco:Collection x:TypeArguments="AssemblyReference">
      <AssemblyReference>mscorlib</AssemblyReference>
      <AssemblyReference>System</AssemblyReference>
      <AssemblyReference>System.Core</AssemblyReference>
      <AssemblyReference>Newtonsoft.Json</AssemblyReference>
      <AssemblyReference>UiPath.System.Activities</AssemblyReference>
    </sco:Collection>
  </TextExpression.ReferencesForImplementation>
  <x:Members>
    <x:Property Name="in_SourceBaseUrl" Type="InArgument(x:String)" />
    <x:Property Name="in_SourceToken" Type="InArgument(x:String)" />
    <x:Property Name="in_DataPath" Type="InArgument(x:String)" />
  </x:Members>
  <Sequence DisplayName="Export Step 4 - Queues">
    <Sequence.Variables>
      <Variable x:TypeArguments="x:String" Name="v_FoldersJson" Default="&quot;&quot;" />
      <Variable x:TypeArguments="x:String" Name="v_OutputPath" Default="&quot;&quot;" />
      <Variable x:TypeArguments="x:String" Name="v_AllQueuesJson" Default="&quot;[]&quot;" />
    </Sequence.Variables>

    <ui:LogMessage Level="Info" Message="&quot;[Export Queues] Reading folder list and fetching queue definitions per folder...&quot;" DisplayName="Log: Start" />

    <ui:InvokeCode DisplayName="Read Export_Folders.json">
      <ui:InvokeCode.Code>
        <![CDATA[
v_FoldersJson = System.IO.File.ReadAllText(
    System.IO.Path.Combine(in_DataPath, "Export_Folders.json"),
    System.Text.Encoding.UTF8)
        ]]>
      </ui:InvokeCode.Code>
    </ui:InvokeCode>

    <ui:InvokeCode DisplayName="Fetch Queue Definitions Per Folder">
      <ui:InvokeCode.Code>
        <![CDATA[
Dim folders As Newtonsoft.Json.Linq.JArray = Newtonsoft.Json.Linq.JArray.Parse(v_FoldersJson)
Dim allQueues As New Newtonsoft.Json.Linq.JArray()

For Each folder As Newtonsoft.Json.Linq.JObject In folders
    Dim folderId As String = folder("Id").ToString()
    Dim folderName As String = If(folder("FullyQualifiedName") IsNot Nothing AndAlso
        folder("FullyQualifiedName").Type <> Newtonsoft.Json.Linq.JTokenType.Null,
        folder("FullyQualifiedName").ToString(),
        folder("DisplayName").ToString())

    Dim httpClient As New System.Net.Http.HttpClient()
    httpClient.Timeout = TimeSpan.FromSeconds(60)
    Dim skip As Integer = 0
    Dim top As Integer = 100
    Dim hasMore As Boolean = True

    Do While hasMore
        Dim url As String = in_SourceBaseUrl.TrimEnd("/"c) &
            "/odata/QueueDefinitions?$top=" & top & "&$skip=" & skip
        Dim request As New System.Net.Http.HttpRequestMessage(System.Net.Http.HttpMethod.Get, url)
        request.Headers.TryAddWithoutValidation("Authorization", "Bearer " & in_SourceToken)
        request.Headers.TryAddWithoutValidation("Accept", "application/json")
        request.Headers.TryAddWithoutValidation("X-UIPATH-OrganizationUnitId", folderId)

        Dim resp As System.Net.Http.HttpResponseMessage = httpClient.SendAsync(request).Result
        If Not resp.IsSuccessStatusCode Then
            hasMore = False
            Continue Do
        End If

        Dim body As String = resp.Content.ReadAsStringAsync().Result
        Dim obj As Newtonsoft.Json.Linq.JObject = Newtonsoft.Json.Linq.JObject.Parse(body)
        Dim items As Newtonsoft.Json.Linq.JArray = TryCast(obj("value"), Newtonsoft.Json.Linq.JArray)

        If items Is Nothing OrElse items.Count = 0 Then
            hasMore = False
        Else
            For Each queue As Newtonsoft.Json.Linq.JObject In items
                queue("_SourceFolderId") = folderId
                queue("_SourceFolderName") = folderName
                allQueues.Add(queue)
            Next
            skip += top
            If items.Count < top Then hasMore = False
        End If
    Loop
Next

v_AllQueuesJson = allQueues.ToString(Newtonsoft.Json.Formatting.Indented)
        ]]>
      </ui:InvokeCode.Code>
    </ui:InvokeCode>

    <Assign DisplayName="Set Output Path">
      <Assign.To><OutArgument x:TypeArguments="x:String">[v_OutputPath]</OutArgument></Assign.To>
      <Assign.Value><InArgument x:TypeArguments="x:String">[System.IO.Path.Combine(in_DataPath, "Export_Queues.json")]</InArgument></Assign.Value>
    </Assign>

    <ui:InvokeCode DisplayName="Save Export_Queues.json">
      <ui:InvokeCode.Code>
        <![CDATA[System.IO.File.WriteAllText(v_OutputPath, v_AllQueuesJson, System.Text.Encoding.UTF8)]]>
      </ui:InvokeCode.Code>
    </ui:InvokeCode>

    <ui:LogMessage Level="Info" Message="[&quot;[Export Queues] Saved &quot; &amp; Newtonsoft.Json.Linq.JArray.Parse(v_AllQueuesJson).Count.ToString() &amp; &quot; queue definitions to &quot; &amp; v_OutputPath]" DisplayName="Log: Complete" />
  </Sequence>
</Activity>
